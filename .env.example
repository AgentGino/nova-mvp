# AWS Configuration
# Specify the AWS profile configured using `aws configure --profile <your-profile-name>`
# If commented out or left blank, the 'default' profile will be used.
AWS_PROFILE=default

# Default region for AWS SDK (optional, but can be useful)
AWS_DEFAULT_REGION=us-east-1

# Bedrock Configuration
# The specific Bedrock model to use for the voice conversation
BEDROCK_MODEL_ID=amazon.nova-sonic-v1:0

# The AWS region where your Bedrock model is hosted
BEDROCK_REGION=us-east-1

# The system prompt instructing the Bedrock model on its persona and task
BEDROCK_SYSTEM_PROMPT="You are a helpful voice assistant named Roja. The user and you will engage in a spoken dialog exchanging the transcripts of a natural real-time conversation. Keep your responses short, generally two or three sentences for chatty scenarios."

# The voice ID for the text-to-speech output (e.g., matthew, joanna, ivy, justin, kendra, kimberly, salli, conrad, ruth)
BEDROCK_VOICE_ID=matthew # tiffany, amy
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=